--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.110.0
uvicorn[standard]==0.28.0

sqlalchemy==2.0.28
psycopg2-binary==2.9.9

pydantic==2.6.4
pydantic-settings==2.2.1
email-validator==2.1.1

python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

python-multipart==0.0.9
pillow==10.2.0

# Linux (Render 512 MB Free tier): CPU-only wheels to avoid ~800MB CUDA bloat
torch==2.2.2+cpu; sys_platform == 'linux'
torchvision==0.17.2+cpu; sys_platform == 'linux'

# Local development (macOS Apple Silicon / Windows): Standard CPU/MPS wheels
torch==2.2.2; sys_platform != 'linux'
torchvision==0.17.2; sys_platform != 'linux'

python-dotenv==1.0.1
httpx==0.27.0
pytest>=8.1.1

numpy==1.26.4